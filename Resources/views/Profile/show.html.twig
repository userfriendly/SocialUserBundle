{% extends '::base.html.twig' %}

{% block body %}

    <div class="container">

        {# PAGE HEADER #}
        <div class="page-header">
            <h1>{{ user.username }}</h1>
        </div>

        <p>E-mail: {{ user.email }}</p>

    </div>

{% endblock %}